#include <iostream>

int main()
{
    // 초기값; 조건식; 증감식 으로 이루어져있고
        // 일정 범위를 내가 반복할때 자주 사용된다.
        for (초기화; 조건; 증감)
        {

        }

    // while (  조건 ) 참일 경우에
    // 해당 반복문을 실행한다.
    while (조건)
    {

    }

    // 최초 한번은 실행후 조건판단을 진행하여
    // 반복문을 실행한다.
    do
    {

    } while (true);

    // 범위 기반 for문
    // 순회기능 특화
    for (int i : arr)
    {

    }

    // break - 가장가까운 루프를 빠져나옴
    // continue - 특정조건에 스킵하고 싶은상황에 사용
}

